# Skills refresh icon

The Installed Skills refresh action had retained its text and behavior but lost the icon element from the page markup. It now uses the same regular Phosphor clockwise-arrow icon as the other refresh actions in the app, without introducing a one-off asset or style.

Validation records the renderer layout test, typecheck, production build, and `git diff --check` in both projects. No package or installation was made.
