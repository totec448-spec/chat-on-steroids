# Recovery, ownership and dependency snapshot

This snapshot applies subsequent workspace changes to public main while retaining
previously integrated fixes and contributor credit. No version bump, release or
updater behavior change is included.

- Unattributed recovery uses the original active cohort and exact request budget,
  with the browser claiming authority after tab discovery and before its action.
- Automatic Loop continuation requires an exactly attributed MCP call in the source
  turn. Recorded turn identity survives reload; explicit activation keeps its own
  existing eligibility policy.
- Accepted fresh-chat receipts survive native promotion to the delivered chat.
  Adopted answers remain scoped to their question, including the legitimate previous
  answer when a newer question arrives in the same observation batch. These focused
  repairs adapt ventianima-lab's work in #185 and its follow-up.
- Unknown-model recovery retains the conservative ten-minute active-work window
  (#172). Plan details start collapsed with their existing native disclosure (#191).
- Runtime/editor/MCP/plugin dependencies and corresponding license/native-source
  inventories are updated. Electron 44 clipboard operations await completion.

Integration resolved overlapping product-map, changelog, contributor, receipt and
Fetch-test edits. The Fetch regression still exercises the existing Windows
installation-path bound while accepting the reviewed release's own dependency pins.
Private local history, forensic documents and session evidence are excluded.

Validation: TypeScript, privacy/staged-tree checks, 93-package license notices,
730 native-source identities and production build pass. The full four-worker test
run passes 4,203 tests with 42 skipped; both isolated shutdown tests pass. The opt-in
Fetch live regression also passes using an isolated loopback fixture.

Separately, the requested local workspace snapshot passes 4,146 tests plus both
shutdown tests. Its Windows x64 package passes Electron 44.3.0, Sharp 0.35.4/libvips
8.18.6, PTY and tree-sitter runtime smoke. Five bundles and fourteen companion files
match their build/source inputs. That installed local snapshot is distinct from this
PR's integration on public main; no live ChatGPT/provider behavior is implied.
